# =============================================================================
# SBI AML Detection Pipeline — Python Package List
# Verified environment: Python 3.9.x / RHEL 9.6 (Air-gapped)
#
# RHEL 9.6 includes Python 3.9 by default — no SCL installation required
#   python3 --version  # Verify: 3.9.x
#
# Note on SDV:
#   sdv depends on ctgan → torch (PyTorch).
#   PyTorch binary wheels are not distributed with standard manylinux tags on PyPI,
#   so pip download --only-binary will fail on non-RHEL machines.
#   Always run pip download on the same RHEL 9.6 Bastion as the cluster.
#
# =============================================================================
# Air-gapped Installation
# =============================================================================
#
# [Step 1] On RHEL 9.6 Bastion machine (needs internet):
#
#   sudo dnf install -y python3-gssapi krb5-devel gcc python3-devel
#   python3 -m venv --system-site-packages /tmp/aml-venv
#   source /tmp/aml-venv/bin/activate
#   pip install --upgrade pip
#
#   # Download packages (no platform flags needed — Bastion matches cluster OS)
#   pip download -r requirements.txt -d ./wheels/
#   tar cf aml-wheels.tar wheels/
#   scp aml-wheels.tar systest@<cluster-host>:/tmp/
#
# [Step 2] On cluster node:
#   sudo dnf install -y python3-gssapi krb5-devel
#   python3 -m venv --system-site-packages /tmp/aml-venv
#   source /tmp/aml-venv/bin/activate
#   cd /tmp && tar xf aml-wheels.tar
#   pip install --no-index --find-links=./wheels/ -r requirements.txt
#
# [Step 3] Always activate venv before running:
#   source /tmp/aml-venv/bin/activate
#
# =============================================================================

sdv>=1.9.0
pandas>=1.5.0
numpy>=1.23.0
kafka-python>=2.0.0
python-snappy>=0.7.0
requests>=2.28.0
